#ifndef FUNCTIONS_H
#define FUNCTIONS_H

// Function declaration
void printWindows();

// Macro declaration
#define ARRAY_SIZE(a) (sizeof(a) / sizeof(a[0]))
#define WINDOWS_SIZE 3

// GLobal variables declaration
WINDOW *windows[WINDOWS_SIZE];

// typedef declaration

#endif