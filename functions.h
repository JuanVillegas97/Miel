#ifndef FUNCTIONS_H
#define FUNCTIONS_H

// Function declaration
void printWindows();

// Macro declaration
#define ARRAY_SIZE(a) (sizeof(a) / sizeof(a[0]))

// Global variables declaration
WINDOW *window;

// typedef declaration

#endif